// Generated by CoffeeScript 1.4.0
var style;

style = function(selector, prop) {
  var cache, child, cid, i, len, parent, part, parts, ret, root;
  cache = style.cache = style.cache || {};
  cid = selector + ":" + prop;
  if (cache[cid]) {
    return cache[cid];
  }
  parts = selector.split(RegExp(" +"));
  len = parts.length;
  parent = root = document.createElement("div");
  child = void 0;
  part = void 0;
  i = 0;
  while (i < len) {
    part = parts[i];
    child = document.createElement("div");
    parent.appendChild(child);
    parent = child;
    if ("#" === part[0]) {
      child.setAttribute("id", part.substr(1));
    } else {
      if ("." === part[0]) {
        child.setAttribute("class", part.substr(1));
      }
    }
    ++i;
  }
  document.body.appendChild(root);
  ret = getComputedStyle(child)[prop];
  document.body.removeChild(root);
  return cache[cid] = ret;
};
